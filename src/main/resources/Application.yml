spring:
  datasource:
    url: jdbc:mysql://localhost:3307/Student_Management?createDatabaseIfNotExist = true
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true